steps:
  - name: 'gcr.io/k8s-skaffold/pack'
    entrypoint: pack
    args: ['build', '$_GCR_HOSTNAME/$PROJECT_ID/$REPO_NAME/$_SERVICE_NAME:$COMMIT_SHA', '--builder=gcr.io/buildpacks/builder:v1', '--network=cloudbuild', '--path=.']
    id: Buildpack
    

    